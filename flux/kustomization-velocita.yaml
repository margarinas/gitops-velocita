apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: velocita
  namespace: flux-system
spec:
  dependsOn:
    - name: databases
    - name: helm
  interval: 1h0m0s
  path: ./apps/velocita
  prune: true
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  sourceRef:
    kind: GitRepository
    name: flux-system
