apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: velocita-db
  namespace: default
spec:
  name: velocita-db
  owner: velocita-db-user
  databaseReclaimPolicy: retain
  cluster:
    name: postgres
  schemas:
    - name: public
      owner: velocita-db-user
