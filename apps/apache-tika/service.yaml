---
apiVersion: v1
kind: Service
metadata:
  name: apache-tika
  namespace: default
  labels:
    app.kubernetes.io/name: apache-tika
spec:
  type: ClusterIP
  ports:
    - port: 9998
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: apache-tika
