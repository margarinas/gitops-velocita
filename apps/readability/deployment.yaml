---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: readability
  namespace: default
  labels:
    app.kubernetes.io/name: readability
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: readability
  template:
    metadata:
      labels:
        app.kubernetes.io/name: readability
    spec:
      imagePullSecrets:
        - name: regcred
      tolerations:
        - key: node.kubernetes.io/server-usage
          operator: Equal
          value: readability
          effect: NoSchedule
      containers:
        - name: readability

          image: "margarinas/readability:latest"
          imagePullPolicy: Always

          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: READABILITY_MAX_CONCURRENCY
              value: "1"
            - name: READABILITY_MAX_HTML_BYTES
              value: "8000000"
            - name: READABILITY_BROWSER_OPERATION_TIMEOUT_MS
              value: "30000"
          resources:
            requests:
              cpu: 200m
              memory: 200Mi
            limits:
              cpu: 3000m
              memory: 6Gi

      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-role.kubernetes.io/control-plane
                    operator: DoesNotExist
                  - key: node-role.kubernetes.io/master
                    operator: DoesNotExist
                  - key: node.kubernetes.io/server-usage
                    operator: In
                    values:
                      - readability
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app.kubernetes.io/name: readability
              topologyKey: kubernetes.io/hostname
